{
	"AWSTemplateFormatVersion" : "2010-09-09",
	"Description" : "Creates all required egress rules for the security groups",
	"Parameters" : {
		"tagStack":{
	    "Type": "String",
	    "Description":"tag - stack name"
	  },
		"appServerSecurityGroup":{
	  	"Description" : "The security group that governs the app server",
	  	"Type":"AWS::EC2::SecurityGroup::Id"
	  },
		"dbServerSecurityGroup":{
	  	"Description" : "The security group that governs the database server",
	  	"Type":"AWS::EC2::SecurityGroup::Id"
	  },
		"elbUtilityVpcSecurityGroup":{
	  	"Description" : "The security group that governs the ELB in the public subnet of utility vpc",
	  	"Type":"AWS::EC2::SecurityGroup::Id"
	  },
		"fileServerSecurityGroup":{
	  	"Description" : "The security group that governs the file server",
	  	"Type":"AWS::EC2::SecurityGroup::Id"
	  },
		"rdGatewaySecurityGroup":{
	  	"Description" : "Security Group for the rd gateway in public subnet",
	  	"Type":"AWS::EC2::SecurityGroup::Id"
	  }
	},

	"Mappings":{},

	"Resources" : {
		"appServerEgressToDbServer":{
			"Type":"AWS::EC2::SecurityGroupEgress",
			"Properties":{
				"DestinationSecurityGroupId" : {"Ref": "dbServerSecurityGroup"},
				"FromPort" : "0",
				"GroupId" : {"Ref":"appServerSecurityGroup"},
				"IpProtocol" : "6",
				"ToPort" : "65535"
			},
			"Metadata":{
				"Comment":"TEMP:  wide open access from app server to db server"
			}
		},
		"appServerEgressToFileServer":{
			"Type":"AWS::EC2::SecurityGroupEgress",
			"Properties":{
				"DestinationSecurityGroupId" : {"Ref": "fileServerSecurityGroup"},
				"FromPort" : "0",
				"GroupId" : {"Ref":"appServerSecurityGroup"},
				"IpProtocol" : "6",
				"ToPort" : "65535"
			},
			"Metadata":{
				"Comment":"TEMP:  wide open access from app server to file server"
			}
		},

		"rdGatewayHttpEgressToInternet":{
			"Type":"AWS::EC2::SecurityGroupEgress",
			"Properties":{
				"CidrIp" : "0.0.0.0/0",
				"FromPort" : "80",
				"GroupId" : {"Ref":"rdGatewaySecurityGroup"},
				"IpProtocol" : "6",
				"ToPort" : "80"
			},
			"Metadata":{
				"Comment":"REVIEW:  Need to limit Destination CIDR Range (maybe just AWS services)"
			}
		},
		"rdGatewayHttpsEgressToInternet":{
			"Type":"AWS::EC2::SecurityGroupEgress",
			"Properties":{
				"CidrIp" : "0.0.0.0/0",
				"FromPort" : "443",
				"GroupId" : {"Ref":"rdGatewaySecurityGroup"},
				"IpProtocol" : "6",
				"ToPort" : "443"
			},
			"Metadata":{
				"Comment":"REVIEW:  Need to limit Destination CIDR Range (maybe just AWS services)"
			}
		}
	},
	"Outputs" : {}
}
